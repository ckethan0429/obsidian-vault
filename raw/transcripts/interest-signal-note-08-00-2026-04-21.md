# Interest Signal Note — 08:00 run (2026-04-21)

- **이번 아침의 가장 또렷한 신규 포인트는 GitHub stars 확장입니다.** 기존 6개 고정 축에서 벗어나 `memory-bank`, `oh-my-agent`, `web-agent`, `pyturboquant` 가 추가되어, 관심이 이제 **에이전트 메모리 계층 + 멀티에이전트 하네스 + 웹 리서치 에이전트 + 양자화/연구 코드** 쪽으로 넓어졌습니다.
- **YouTube는 `운영형 에이전트 + 배포 표면 확장` 신호가 강했습니다.** `How Anthropic uses Claude in Product Engineering`, `Running LLMs on your iPhone: 40 tok/s Gemma 4 with MLX`, `Codex 2.0: Automate ANYTHING!`, `Hermes Agent Workspace + Claude Code Local!` 이 함께 보여서, CK님의 레이더가 **Anthropic 내부 운영 방식 + on-device/open-weight 실행 + Codex/Claude 로컬 워크스페이스** 쪽으로 확장되고 있습니다.
- **X / Threads는 프롬프트 팁보다 `하네스/런타임/재시작성` 쪽으로 한 단계 더 이동했습니다.** X likes에는 `Agent Lightning`, `autoresearch with claude code`, `One(47,000 agentic actions)` 이 보였고, Threads에는 `Restartable cattle`, `Brain / Hands / Memory`, `Managed Agents`, `pgAI`, `mem0 + Qdrant + Ollama` 가 이어져서 **에이전트를 서비스처럼 굴리는 운영 레이어**가 계속 중심축입니다.
- **Google / ChatGPT / Claude는 밤사이 `Threads API + Meta/ngrok + 에디터/프로젝트 통합` 축을 강화했습니다.** Google 검색 기록에 `threads api`, Meta 개발자 문서, `ngrok` 이 찍혔고, ChatGPT는 `공유 메모리 에이전트`, `Codex 플러그인`, `ACP Editor Integration` 을, Claude는 `관심 신호에서 패턴 찾기`, `M365 Planner와 GitHub Project 연동` 을 보여서 **브라우저 신호 수집 → 위키 승격 → IDE/PM 시스템 연결** 쪽이 실제 작업 축으로 보입니다.
- **전달 상태:** Discord 채널 `1490692905604481026` 는 이번에도 라이브 Chrome/CDP 세션에서 `discord.com/login` 으로 열려 직접 게시하지 못했습니다. 이 note는 로컬에 저장해 두었습니다.
