# Interest Signal Note — 08:00 run (2026-04-23)

- **Threads는 오늘 아침에도 어젯밤 19:00의 `AI infra operating economics` 논리를 그대로 유지/강화하고 있습니다.** `/mentions` 는 이번에도 빈 배열이었고, `keyword_search` 는 `Claude Code`, `agent`, `memory`, `GPU`, `Proxmox`, `Ceph` 에만 실제 hit가 났습니다. 최신 own-post insight 기준으로 `AI 인프라의 진짜 승부처` 는 **736 views / 1 like / 7 replies / 1 repost** 까지 올라와 있고, `에이전트 생산성의 진실` 은 여전히 **3023 views / 13 likes / 7 replies / 2 reposts / 1 quote** 로 가장 강한 anchor입니다.
- **Threads liked page 상단은 여전히 배포/컴퓨트/제품화 축으로 수렴합니다.** 직접 보인 liked rows는 `@feeltech__` 의 **“쿠버네티스는 이제 AI의 사실상 운영체제”**, `@ai_ethan_sns` 의 **Gemini Mac 앱 / 며칠 만의 Swift 프로토타이핑**, `@unclejobs.ai` 의 **Anthropic-AWS 10년 1,000억 달러 / 5GW 컴퓨트 베팅**, `@choi.openai` 의 **Codex 400만 돌파로 사용량 리셋** 이었습니다. 즉 Threads liked feed도 모델 스펙보다 **AI OS / 앱 제작 속도 / 컴퓨트 자본 / 사용량 관리** 쪽으로 굳어져 있습니다.
- **YouTube는 이번 아침에 더 실무형 툴·런타임 쪽으로 바뀌었습니다.** 상단에서 실제로 보인 건 `Space Agent: New Autonomous AI Agent`, `ChatGPT Images 2.0 + Space Agent`, `AWS Lambda durable functions: Best Practices, AI patterns, and Futures`, `Opus 4.7 VS GPT-5.4 VS Kimi K2.6 Code!` 였습니다. 어제 Ceph/Kubernetes/Anthropic megadeal이 강했다면, 오늘 아침은 **autonomous agent tool + serverless runtime + coding-model 비교** 쪽이 더 선명합니다.
- **X likes는 구조/학습/효율화 레이어를 더 또렷하게 보여줍니다.** 최상단에 `freeCodeCamp graphs` 와 `Every pipeline ... was a graph / DAG` 가 같이 보였고, 이어서 `I Replaced Karpathy's LLM Wiki`, `10 GitHub repos`, `RAG vs. CAG`, `Prompt caching`, `I stopped hitting Claude's usage limits` 가 붙었습니다. 즉 CK님의 관심이 단순 에이전트 하입보다 **graph/orchestrator 구조, 위키/지식 시스템, repo-first 학습, 캐시/토큰 효율화** 쪽으로 더 정밀해진 모습입니다.
- **Google 기록은 오늘 아침에 새로운 사업/벤더 탐색층을 추가했습니다.** 맨 위에 `스마트스토어 판매자센터`, `천유닷컴`, `proxlb`, `엠피씨`, `엠피씨플러스` 가 올라와 있었고, 그 아래에는 그대로 `Kimi K2.6`, `Claude Code + Telegram agent + mem0 + Qdrant + Ollama`, `openclaw`, `xterm.js`, `Threads API`, `ngrok` 이 이어졌습니다. GitHub stars는 계속 13개(`atomic-hermes`, `hermes-agent`, `OmniVoice`, `memory-bank`, `oh-my-agent` 등)로 유지됐고, ChatGPT/Claude는 여전히 **ACP editor runtime + shared memory wiring + AI infra distribution / GPU business packaging** 축을 반복했습니다.
- **전달 상태:** Discord 채널 `1490692905604481026` 는 이번에도 `discord.com/login?redirect_to=%2Fchannels%2F1490692905604481026%2F1490692905604481026` 로 열렸고, 로그인 폼(`이메일 또는 전화번호`, `비밀번호`, `로그인`)이 노출되어 직접 게시하지 못했습니다. 이 note를 로컬에 저장해 두었습니다.
