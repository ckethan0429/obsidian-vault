# Interest Signal Note — 08:00 rerun (2026-04-21, Threads API)

- **Threads는 이번 런에서 공식 API로 다시 확인했고, strongest signal은 그대로 `Managed Agents / Brain·Hands·Memory / Restartable cattle / Security boundary / routing·recovery·orchestration` 축이었습니다.** 다만 `/mentions` 와 `keyword_search` 는 둘 다 `앱에 액세스 티어 접근 권한이 없습니다` 로 막혔고, likes 전용 surface도 API에 직접 보이지 않아, 이번 Threads 평가는 **own posts + replies** 기준입니다.
- **GitHub stars는 여전히 이번 날짜 기준 가장 선명한 확장 신호입니다.** `memory-bank`, `oh-my-agent`, `web-agent`, `pyturboquant` 가 기존 `claude-code-best-practice`, `hermes-workspace`, `legal-agent-orchestrator`, `oh-my-codex`, `obsidian-social-archiver-releases`, `claude-code-telegram` 위에 추가되어, 관심축이 **에이전트 메모리 계층 + 멀티에이전트 하네스 + 웹 리서치 에이전트 + 양자화/연구 코드** 쪽으로 넓어져 있습니다.
- **YouTube / X는 프롬프트 팁보다 운영형 에이전트 레이어를 계속 강화하고 있습니다.** YouTube에는 `How Anthropic uses Claude in Product Engineering`, `Running LLMs on your iPhone: Gemma 4 with MLX`, `Codex 2.0`, `Hermes Agent Workspace + Claude Code Local!`, `Claude Code Local DESTROYS Claude Code?` 가 함께 보였고, X에는 `Claude sessions + NotebookLM`, `Claude Code hidden features`, `One (47,000 agentic actions)`, `Agent Lightning`, `autoresearch with claude code` 가 묶여 있습니다.
- **Google / ChatGPT / Claude는 `Threads API + Meta/ngrok + shared memory + ACP/editor integration + PM/tool orchestration` 축을 전면화합니다.** Google 기록엔 `threads api`, Meta docs, `ngrok`, `anthropic managed agent`, `claude code discord`, `hermes agent` 가 실제로 남아 있고, ChatGPT는 `공유 메모리 에이전트`, `Codex 플러그인`, `ACP Editor Integration` 에서 메모리 브로드캐스트/플러그인/IDE 내 에이전트 서버를 다루고 있으며, Claude는 `관심 신호에서 패턴 찾기`, `Meta 스레드 API 리다이렉트 URL 설정 오류`, `M365 Planner와 GitHub Project 연동` 으로 **수집 → 위키 승격 → PM/운영 체계 연결** 쪽을 밀고 있습니다.
- **전달 상태:** Discord 채널 `1490692905604481026` 는 이번에도 라이브 Chrome/CDP 세션에서 `discord.com/login` 으로 열려 직접 게시하지 못했습니다. 이 note는 로컬에 저장해 두었습니다.
