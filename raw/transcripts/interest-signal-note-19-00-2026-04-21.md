# Interest Signal Note — 19:00 run (2026-04-21)

- **이번 19:00 런의 가장 선명한 신규 포인트는 Threads own post/reply 체인이 `보리스식 병렬 운영 습관`을 아주 구체적으로 풀어냈다는 점입니다.** `에이전트 생산성의 진실` 본문과 이어지는 `1/5 병렬 실행`, `2/5 환경 분리`, `3/5 알림 관리`, `4/5 공용 규칙`, `5/5 셀프 검증과 설계`가 함께 보여서, 관심축이 추상적인 `Managed Agents` 설명에서 한 단계 더 내려와 **worktree 격리, event-based 알림, CLAUDE.md 규칙 축적, self-verification 루프** 같은 운영 디테일로 옮겨갔습니다.
- **YouTube는 여전히 인프라/에이전트 운영 축이 중심이지만, 이번에는 그 표면이 더 명시적이었습니다.** `System Design Course – APIs, Databases, Caching, CDNs, Load Balancing & Production Infra`, `Running LLMs on your iPhone: Gemma 4 with MLX`, `How Anthropic uses Claude in Product Engineering`, `Codex 2.0: Automate ANYTHING!` 위에 `클로드 코드로 100% 자동화 마케팅 에이전트 팀을 만드는 방법 (서브에이전트+스킬+MCP 모두)`, `Full Workshop: Build Your Own Deep Research Agents` 가 같이 보여서 **subagents / MCP / deep research / product-engineering runtime** 관심이 한 묶음으로 보였습니다.
- **X는 신규보다 강화 신호였습니다.** likes/reposts 모두 `Claude sessions + NotebookLM`, `Claude Code 숨겨진 15가지 파워 기능`, `One (47,000 agentic actions)`, `Agent Lightning`, `autoresearch with claude code` 축이 유지되어, CK님 레이더가 여전히 **agent runtime 개선 + 실전 생산성 + 대규모 action surface** 쪽에 고정돼 있음을 보여줍니다.
- **GitHub / Google / ChatGPT / Claude는 같은 축을 다른 표면에서 재확인했습니다.** GitHub stars 10개는 `memory-bank`, `oh-my-agent`, `web-agent`, `pyturboquant`, `hermes-workspace`, `oh-my-codex`, `claude-code-telegram` 그대로였고, Google 검색 기록은 `threads api`, Meta 개발자 문서, `ngrok`, `anthropic effective harness for long running agents` 를 계속 보여줬습니다. ChatGPT는 `공유 메모리 에이전트`, `ACP Editor Integration` 에서 **mem0 + Qdrant + Ollama + Stop hook + editor-native ACP + ephemeral session** 을 강조했고, Claude recent/sidebar는 `Meta 스레드 API 리다이렉트 URL 설정 오류`, `관심 신호에서 패턴 찾기`, `M365 Planner와 GitHub Project 연동` 으로 **수집·메모리·프로젝트 운영 연결** 축을 다시 보여줬습니다.
- **전달 상태:** Discord 채널 `1490692905604481026` 는 이번에도 라이브 Chrome/CDP 세션에서 `discord.com/login` 으로 열려 직접 게시하지 못했습니다. 이 short note는 로컬에 저장해 두었습니다.
