# Interest Signal Note — 19:00 run (2026-04-22)

- **이번 19:00 런의 가장 선명한 신규 포인트는 Threads own-post/reply 체인이 드디어 `운영경제성` 언어로 내려왔다는 점입니다.** 새 글 `AI 인프라의 진짜 승부처` 와 이어진 `1/7~7/7` 에서 `Kimi K2.6 = A100 8장 / 월 1,500`, `Anthropic/AWS 베팅`, `Ceph`, `Claude Code + Aurora DSQL`, `GPU 유휴시간`, `Ceph·Proxmox·GPU 인프라 감각` 이 한 묶음으로 나왔습니다. 오전의 liked evidence가 저녁에는 CK님 own-post 논리 체인으로 재가공된 셈입니다.
- **Threads API surface도 이번엔 더 풍부했습니다.** `/mentions` 는 여전히 빈 배열이었지만, `keyword_search` 는 `Claude Code`, `GPU`, `Proxmox`, `Ceph`, `agent`, `memory` 에 실제 hit가 났고, 새 root post `AI 인프라의 진짜 승부처` 는 아주 짧은 시간에 **502 views / 7 replies** 를 기록했습니다. 반면 `Hermes`, `MCP`, `vLLM`, `ZFS`, `Threads API` 는 이번 query set에서 hit가 없었습니다.
- **Threads liked page 상단도 오전과 결이 달랐습니다.** 이번엔 `@feeltech__` 의 **“쿠버네티스는 이제 AI의 사실상 운영체제”**, `@ai_ethan_sns` 의 **Gemini Mac 앱 / 며칠 만의 Swift 프로토타이핑 속도**, `@unclejobs.ai` 의 **Anthropic-AWS 10년 1,000억 달러 / 5GW 컴퓨트 베팅**, `@choi.openai` 의 **Codex 400만 돌파** 가 직접 보였습니다. 즉 liked feed도 모델 점수보다 **배포 OS, 제품화 속도, 컴퓨트 자본** 쪽으로 기울었습니다.
- **GitHub stars는 오전의 11개에서 13개로 늘었고, 상단이 `AtomicBot-ai/atomic-hermes`, `NousResearch/hermes-agent` 로 바뀐 점이 신규입니다.** 기존 `memory-bank`, `oh-my-agent`, `web-agent`, `hermes-workspace`, `legal-agent-orchestrator` 위에 Hermes 본체/파생체가 더 직접적으로 올라와서, CK님 레이더가 지금 **Hermes-centric runtime + workspace + memory + multi-agent harness** 쪽으로 더 가까워졌다는 신호로 읽힙니다.
- **ChatGPT/Claude/Google까지 합치면 이번 저녁 축은 ‘에이전트’보다 ‘운영 가능한 AI 인프라 스택’에 더 가깝습니다.** ChatGPT는 계속 **shared memory wiring + Stop hook + provider pinning + ACP editor runtime** 을 강조했고, Claude는 **AI Infrastructure Distribution / docker→installer→AI OS / GPU-as-a-Service 플랫폼** 을 밀었습니다. Google 기록 상단은 `스마트스토어 판매자센터`, `천유`, `proxlb`, `엠피씨` 로 사업/공급 탐색이 올라왔고, 아래쪽에는 `Kimi K2.6`, `Claude Code + Telegram agent + mem0 + Qdrant + Ollama`, `Threads API`, `openclaw`, `xterm.js` 가 그대로 남아 있었습니다.
- **전달 상태:** Discord 채널 `1490692905604481026` 는 이번에도 `discord.com/login?redirect_to=%2Fchannels%2F1490692905604481026%2F1490692905604481026` 로 열렸고, 로그인 폼(`이메일 또는 전화번호`, `비밀번호`, `로그인`)이 노출되어 직접 게시하지 못했습니다. 이 note를 로컬에 저장해 두었습니다.
