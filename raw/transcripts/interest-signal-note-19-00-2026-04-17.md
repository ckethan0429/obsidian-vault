# 19:00 Interest Signal Note — pending Discord delivery

- CK님, 이번 저녁 런의 **상대적 신규 신호**는 YouTube 구독 피드의 `System Design Course – APIs, Databases, Caching, CDNs, Load Balancing & Production Infra` 입니다. 오전에 잡히던 Claude Code/Hermes 축 위에, 이번에는 **시스템 설계·프로덕션 인프라 학습 축**이 더 분명하게 올라왔습니다. 같은 묶음으로 `Build AI Voice Agents with Gemini 3.1 Flash TTS`, `MCP Had a Huge Problem in Production… Google Just Fixed It`, `#LangGraph 빌더를 위한 올인원 프론트엔드`, `PaperClip + Hermes Agent + Gemma 4`가 계속 보입니다.
- Google 검색 기록의 새 줄은 **브라우저/CDP·에이전트 운용 레이어** 쪽입니다. 오전부터 이어진 `anthropic effective harness for long running ag...`, `anthropic managed agent`, `claude dangerously skip permissions`, `claude code discord`에 더해, 이번 뷰에는 `honcho`, `comet cdp`가 새로 보였습니다. 관심이 단순 사용법을 넘어 **장기 실행 에이전트 하네스 + 브라우저 제어/연동**으로 더 이동한 신호로 읽힙니다.
- ChatGPT recent titles 쪽 신규 신호는 **실행 세부 레이어**입니다. `공유 메모리 에이전트`, `ACP Editor Integration`, `Power usage analysis tool`, `NVIDIA GPU 모델 확인`, `Discord Target Normalization`가 한 화면에 묶여 있었고, fresh page에서는 assistant 본문이 비어 있거나 `Thinking` 상태였지만 제목과 사용자 프롬프트만으로도 **메모리/에디터 통합/GPU 판별/디스코드 타깃 정규화** 같은 운영 문제에 관심이 옮겨가고 있음을 보여줍니다.
- Claude 쪽 신규 강신호는 **기획-실행 통합 툴링 + 온프레 배포 자동화**입니다. `소프트웨어 개발 기획 통합 관리 도구`, `프로젝트 관리를 위한 AI 도구 추천` 대화에서 `Notion + Linear + v0 + Cursor` 조합이 반복됐고, `온프레 PVE 서버 자동 배포 구성`에서는 `GitHub Actions self-hosted runner`, `webhook`, `Coolify`, 그리고 **폰 음성 → STT → HTTPS POST → 서버 클립보드** 파이프라인까지 나왔습니다. 즉, 관심이 AI 자체보다 **AI를 쓰는 개발 운영체계**로 계속 수렴합니다.
- X likes/reposts, Threads activity/reposts, GitHub stars는 이번에도 **강화 신호** 쪽이었습니다. `Claude sessions`, `Claude Code hidden features`, `One`, `Hermes + Codex + OmX`, `H200`, `vllm`, `legal-agent-orchestrator`, `claude-code-telegram` 같은 축은 유지됐고, 이번 뷰에서 새 GitHub star나 뚜렷한 새 X liked post는 확인되지 않았습니다. 제외 메모: 스포츠/엔터(축구, 크로스핏, 음악/예능)는 요약에서 제외했고, Threads direct likes는 웹에 직접 노출되지 않아 `activity + reposts + profile`로 대체했습니다.

> Note: Discord web session in the live Chrome/CDP profile is currently logged out and redirects to login, so this note was saved locally instead of being posted from inside the browser session to channel `1490692905604481026`.
