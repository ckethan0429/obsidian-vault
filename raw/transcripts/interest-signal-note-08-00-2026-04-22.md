# Interest Signal Note — 08:00 run (2026-04-22)

- **가장 선명한 신규 포인트는 GitHub stars가 10개에서 11개로 늘며 `k2-fsa/OmniVoice` 가 추가된 점입니다.** 기존의 `memory-bank`, `oh-my-agent`, `web-agent`, `hermes-workspace`, `oh-my-codex` 축 위에 **voice cloning / TTS** 가 얹히면서, CK님의 관심이 에이전트 메모리·하네스·웹 리서치에서 **멀티모달 출력 계층**까지 넓어지는 신호로 보입니다.
- **X likes는 오늘 아침 기준으로 운영/구조 레이어 쪽으로 확실히 이동했습니다.** 상단에 `Every pipeline ... was a graph`, `I Replaced Karpathy's LLM Wiki`, `If I had to become an AI engineer in 90 days`, `RAG vs. CAG`, `Prompt caching`, `Claude usage limits` 가 함께 보여서, 관심사가 단순 툴 소개보다 **오케스트레이션 구조·위키/지식 시스템·실전 repo 기반 학습·캐시/토큰 효율화** 쪽으로 재정렬된 모습입니다.
- **YouTube는 어제보다 인프라/스토리지/서버리스 색이 더 강해졌습니다.** `China's Kimi K2.6 Agent Swarms are INSANE!`, `Hermes Agent + Gemma 4`, `Keynote - State of Ceph`, `Closer than you think: Ceph's journey to nearest neighbor search`, `Build a serverless app with Claude Code and Aurora DSQL`, `System Design Course` 가 같이 보여서, CK님의 레이더가 **에이전트 런타임뿐 아니라 저장소·분산 인프라·서버리스 구현면**까지 확장되고 있습니다.
- **Threads API는 여전히 own posts + replies 기준으로만 읽히지만, 강신호는 계속 명확합니다.** `에이전트 생산성의 진실`, `1/5 병렬 실행`, `2/5 환경 분리`, `3/5 알림 관리`, `4/5 공용 규칙`, `5/5 셀프 검증과 설계`가 그대로 보였고, `/mentions` 와 `keyword_search` 는 이번에도 앱 access tier 부족으로 전부 막혔습니다. 즉, Threads는 **Boris식 병렬 운영 습관 / worktree 격리 / CLAUDE.md 규칙 축적 / self-verification** 패턴을 계속 강화하는 쪽입니다.
- **Google·ChatGPT·Claude는 수집→메모리→에디터/운영 연결축을 다시 확인시켜 줍니다.** Google 기록에는 `Kimi K2.6`, `Claude Code + Telegram agent + mem0 + Qdrant + Ollama`, `Threads API`, `Meta`, `ngrok`, `openclaw`, `xterm.js` 가 남아 있었고, ChatGPT는 `ACP Editor Integration` / `공유 메모리 에이전트` 에서 **ACP·shared memory·Stop hook broadcast** 를 다뤘으며, Claude는 `Meta 스레드 API 리다이렉트 URL 설정 오류` 와 `소프트웨어+교육비 패키징` 답변으로 **운영 설정 + 사업화 포지셔닝** 축을 보여줬습니다.
- **전달 상태:** Discord 채널 `1490692905604481026` 는 이번에도 라이브 Chrome/CDP 세션에서 `discord.com/login` 으로 열려 직접 게시하지 못했습니다. 이 note를 로컬에 저장해 두었습니다.
