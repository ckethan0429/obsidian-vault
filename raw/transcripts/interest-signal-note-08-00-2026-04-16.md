# 08:00 Interest Signal Note — pending Discord delivery

- 유튜브/X/Threads에서 새로 강하게 겹친 축은 **Claude Code·병렬 에이전트·autoresearch·agent integration** 입니다. `The new Claude Code desktop app, redesigned for parallel agents`, `I want to extend my Claude sessions`, `Introducing One`, `Agent Lightning`, `how to set up autoresearch with claude code`가 같은 결을 가리킵니다.
- ChatGPT/Claude에서는 관심 축이 여전히 **AI 인프라 제품화**로 수렴합니다. ChatGPT는 `GPU passthrough`, `CUDA toolkit`, `NCCL`, `vLLM`, `AI Infrastructure Engineer` 쪽으로 읽히고, Claude는 `AI Infrastructure Distribution`, `GPU 서버 → 10분 안에 AI 서버`, `docker run ai-stack`, `curl install-ai-stack.sh` 같은 배포판 전략을 전개합니다.
- Threads reposts와 GitHub stars는 **Hermes 중심 원격 운영 + 지식 운영 스택** 신호를 계속 강화합니다. `hermes-workspace`, `oh-my-codex`, `claude-code-telegram`, `obsidian-social-archiver`, 그리고 `tailscale + ssh + tmux` 조합이 한 묶음으로 보입니다.
- Google 검색 기록은 리서치 의도를 더 직접적으로 보여줍니다. `anthropic effective harness for long running ag...`, `anthropic managed agent`, `claude dangerously skip permissions`, `claude code discord` 검색이 확인되어, 관심이 단순 사용법을 넘어 **agent harness/권한/연동 설계** 쪽으로 기울어 있습니다.
- 제외/가용성 메모: YouTube의 챔피언스컵·MLB·손흥민·쇼츠 등 스포츠/엔터 항목은 제외했습니다. Threads는 direct likes가 웹에 노출되지 않아 `activity`+`reposts`로 대체했고, Discord 웹 세션은 로그인 페이지로 열려 브라우저 내부 직접 게시에는 실패했습니다.

> Note: Discord web session in the live Chrome/CDP profile is currently logged out and redirects to login, so this note was saved locally instead of being posted from inside the browser session to channel `1490692905604481026`.
